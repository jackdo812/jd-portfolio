import loading from '../assets/gifs/Loading-leaves.gif';

const Loading = () => {
    
    return (
        <img className="loading w-[256px] h-[256px] sm:w-[512px] sm:h-[512px]" src={loading} alt="Loading" id="loading" />
    )

}

export default Loading
